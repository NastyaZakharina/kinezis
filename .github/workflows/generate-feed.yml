name: Generate Hotline XML Feed

on:
  push:
    branches: [main]
    paths:
      - 'data.js'
      - 'scripts/generate-feed.js'
      - 'scripts/generate-merchant-feed.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate feeds
        run: |
          node scripts/generate-feed.js
          node scripts/generate-merchant-feed.js

      - name: Commit feeds
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add hotline.xml merchant.xml
          git diff --cached --quiet || git commit -m "Auto-update hotline.xml and merchant.xml [skip ci]"
          git push
